# ПОВНИЙ ПРОМТ ДЛЯ REPLIT-АГЕНТА — усі зміни сайту "Gathering Of The Fallen"

Привіт! Це ВЖЕ ГОТОВИЙ і працюючий сайт гурту "Gathering Of The Fallen"
(монорепо; сайт у `artifacts/web` — React + Vite + TypeScript; сервер у
`artifacts/api-server`).

ГОЛОВНЕ ПРАВИЛО: НЕ переробляй з нуля, НЕ міняй дизайн, НЕ чіпай оплату
(Stripe). Зроби ТІЛЬКИ те, що описано нижче. Оплату PayPal НЕ додаємо.

Разом із цим промтом я прикріплюю 8 файлів:
КОД (4 файли): Layout.tsx, SocialLinks.tsx, Music.tsx, translations.ts
ФОТО (4 файли): oleksandr.jpg, tetiana.jpg, yaroslav.jpg, daryna.jpg

================================================================
ЗАВДАННЯ 1. ЗАМІНИТИ 4 ФАЙЛИ КОДУ
================================================================
Заміни вміст цих файлів на прикріплені версії, за ТОЧНИМИ шляхами:

- Layout.tsx        →  artifacts/web/src/components/Layout.tsx
- SocialLinks.tsx   →  artifacts/web/src/components/SocialLinks.tsx
- Music.tsx         →  artifacts/web/src/pages/Music.tsx
- translations.ts   →  artifacts/web/src/i18n/translations.ts

Більше нічого в цих файлах не міняй. У цих версіях уже зроблено:
1) Прибрано мерехтливу "сітку" (ефект ЕЛТ-екрана) по всьому сайту —
   з `Layout.tsx` видалено підключення компонента `CRTScanline`.
2) Виправлено посилання: YouTube → `@gathering-of-the-fallen`,
   Facebook → `share/1BJU7ec7ft/` (у футері та в `SocialLinks.tsx`).
3) У розділ "Музика" додано 6 нових пісень (найновіші — першими), з
   описами та перекладами укр/англ/фр: Підіймай Вогонь, Чуже Лице,
   Нічний Снайпер, Бас і Дим, Блукаючий Козак, Псалми.
4) Головним синглом угорі сторінки поставлено "У танці з попелом", а
   попередній сингл "Вогонь в руках" перенесено в список треків.

Примітка: після заміни `Layout.tsx` компонент
`artifacts/web/src/components/CRTScanline.tsx` більше не використовується —
просто залиш його як є, нічого видаляти не треба.

================================================================
ЗАВДАННЯ 2. ФОТО УЧАСНИКІВ ГУРТУ
================================================================
Зроби фото учасників легко замінними:

1) Створи папку `artifacts/web/public/band/`.

2) Поклади в неї 4 прикріплені фото з такими іменами (вони вже правильні):
   `oleksandr.jpg`, `tetiana.jpg`, `yaroslav.jpg`, `daryna.jpg`.

3) Відредагуй `artifacts/web/src/pages/About.tsx`: заміни масив `IMAGES`
   на сталі шляхи й прибери старі імпорти фото учасників із `@assets`:
   ```ts
   const IMAGES = ["/band/oleksandr.jpg", "/band/tetiana.jpg", "/band/yaroslav.jpg", "/band/daryna.jpg"];
   ```
   Імпорти двох верхніх фото (`topPhoto1` / `topPhoto2`) НЕ чіпай.
   Порядок має лишитися тим самим, що й порядок у `about.members`:
   Олександр, Тетяна, Ярослав, Дарина. Верстку не змінюй.

================================================================
ПЕРЕВІРКА І ЗВІТ
================================================================
- Переконайся, що збірка `artifacts/web` проходить без помилок.
- Перевір: мерехтлива сітка зникла; у "Музиці" видно нові пісні й новий
  головний сингл "У танці з попелом"; на "Про гурт" у кожного учасника
  правильне фото; посилання YouTube/Facebook правильні.
- Оплата Stripe має працювати, як і раніше (її не чіпали).

Коли все зробиш, напиши мені:
- що готово;
- що для заміни фото будь-якого учасника надалі я просто перезаписую файл
  у `public/band/` з тим самим іменем (без правок коду).
